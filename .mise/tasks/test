#!/usr/bin/env bash
#MISE description="Run the framework's tests"
#MISE dir="{{ config_root }}"
set -euo pipefail

printf '\n== bats ==\n'
bats --print-output-on-failure test

printf '\n== skeleton ==\n'
for f in skeleton/house/hooks/* skeleton/house/.mise/tasks/*; do
  bash -n "$f"
done
echo ok
