#!/usr/bin/env bash
#MISE description="Run the framework's tests"
#MISE hide=true
#MISE dir="{{ config_root }}"
set -euo pipefail

printf '\n== bats ==\n'
bats --print-output-on-failure test

printf '\n== scaffold ==\n'
for f in scaffold/house/hooks/* scaffold/house/.mise/tasks/*; do
  bash -n "$f"
done
echo ok
