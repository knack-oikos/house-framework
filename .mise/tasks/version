#!/usr/bin/env bash
#MISE description="Print the framework version: the tag at HEAD, else the short commit, else unknown"
set -euo pipefail

source "$MISE_CONFIG_ROOT/lib/house.sh"

framework_version
