#!/usr/bin/env bash
#MISE description="Run the household's own checks"
#MISE dir="{{ config_root }}"
set -euo pipefail

printf '\n== bats ==\n'
bats --print-output-on-failure test

printf '\n== welcome smoke ==\n'
mise run -q welcome >/dev/null
echo ok
